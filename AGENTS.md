# Vocabatron 开发边界

- 只在用户明确授权的现有本地项目目录执行，先核对实际环境与 Git 状态。
- 仅使用 main；不暂存、提交、推送、修改远端、创建其他分支或 worktree。
- 不以云端开发、远程克隆或其他工作区替代本地执行。
- 保留现有工作；冲突、原文变化或规则变更须先向用户编号询问。
- 私人原件只读；私人配置、转录、词表、模型响应和全部结果放入忽略的 .private/。
- 公开代码、文档和测试只能包含通用实现与自行构造的合成材料。
- 模型仅使用已安装的本机 gemma4:31b，不下载模型，不使用远程回退。
- 先独立验证布局，再验证最终 PDF 内容和渲染，最后原子发布完整双份结果。
- 默认测试离线；真实私人集成必须显式启用。
- 本阶段只实施核心、CLI 和本地验收；完成后停止，不部署 Web/PWA 或系统服务。
- 始终中文沟通；需要新提示词时在聊天中给出完整连续文本，不生成下载文件。
